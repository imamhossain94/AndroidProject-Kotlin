apply plugin : 'com.android.library'
apply from : '../../common.gradle'

android {

    defaultConfig {
        //Module obfuscation configuration
        consumerProguardFiles 'proguard-base.pro'
    }

    sourceSets {
        main {
            // res resource directory configuration
            res.srcDirs(
                    'src/main/res',
                    'src/main/res-sw',
            )
        }
    }
}